import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#2462d4',
      dark: '#ffffff',
    },
    // type: 'dark',
    secondary: {
      main: '#28282c',
    }
  },
});

export default theme